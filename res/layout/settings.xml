<?xml version="1.0" encoding="utf-8"?>
<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:layout_gravity="top"
    android:stretchColumns="1"
    android:background="#ffffff">
        <TableRow android:id="@+id/tempUnitRow" android:background="@drawable/background">
        <LinearLayout android:id="@+id/tempUnitContainer"
            android:orientation="vertical"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="10dp">
            <TextView
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:text="Temperature Units"
                android:textColor="#333"
                android:textSize="16sp"
            />
            <TextView
                android:id="@+id/tempUnitValue"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:text=""
                android:textColor="#333"
                android:textSize="12sp"
                android:padding="4dp"
            />
        </LinearLayout>
        <ImageView
            android:id="@+id/tempUnitMore"
            android:src="@drawable/ic_menu_more"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="10dp"
            android:layout_gravity="right"
            android:gravity="right"
            android:layout_toRightOf="@id/tempUnitContainer"
            android:layout_alignTop="@id/tempUnitContainer"
        
        />
        </TableRow>
</TableLayout>
